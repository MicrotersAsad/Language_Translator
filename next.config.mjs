const nextConfig = {
    reactStrictMode: true,
    experimental: {
      appDir: true, // ✅ নিশ্চিত করুন এটি `true` আছে
    },
  };
  
  export default nextConfig; // ✅ `module.exports` পরিবর্তন করে `export default` ব্যবহার করুন
  